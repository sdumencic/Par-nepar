import React from 'react';
import './index.css';

const Nepar = (props) => {
    return (
        <button
            className="Btn"
            >{props.label}</button>
    );
}

export default Nepar;